# BabyVoice Museum - Python dependencies (Windows desktop)
# Core audio I/O & DSP
numpy>=1.24
scipy>=1.10
soundfile>=0.12.1
sounddevice>=0.4.6
librosa>=0.10.0

# Real-time lightweight denoising
# rnnoise is an optional compiled binding; noisereduce is the always-available fallback
noisereduce>=2.2.0

# Offline Chinese ASR with word-level timestamps
vosk>=0.3.45

# GUI & visualization
PySide6>=6.6.0
pyqtgraph>=0.13.3

# Utilities
python-dateutil>=2.8.2
